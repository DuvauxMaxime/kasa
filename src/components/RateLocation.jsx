import styles from './RateLocation.module.css';
import star from '../assets/star.png';

const RateLocation = () => {
   return (
      <span>
         <img src={star} alt="étoile notation" />
      </span>
   );
};

export default RateLocation;
